import { ILanguage } from './interface';

const language: ILanguage = {
  app: {
    company: 'Incognito',
  },
  nodeMonitor: {},
  reuse: {
    search: 'Search',
    colon: ':',
  },
};

export default language;
